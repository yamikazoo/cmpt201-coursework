#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>
#define _POSIX_C_SOURCE 200809L

int main() {
  char command;
  printf("Enter programs to run. \n");
  getline(command, 100, stdin);
  execl(command, 0);
}
